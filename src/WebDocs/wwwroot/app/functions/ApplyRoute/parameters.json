[
  {
    "Name": "Url",
    "Description": "The path to navigate to. It is matched (case-insensitively) against the regular expressions of the configured routes; the first matching route is applied and its Expression executed. Returns false when no route matches or the route is rejected",
    "Types": [ "text", "mustache" ],
    "Optional": false
  },
  {
    "Name": "IsLoad",
    "Description": "When true, also executes the route's BeforeLoadExpression and AfterLoadExpression, as happens when the route is applied at startup",
    "Types": [ "conditional" ],
    "Optional": true,
    "DefaultValue": "false"
  }
]
